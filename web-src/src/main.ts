import './styles/global.css';
import './app.ts';

// Lit component registrations happen via @customElement decorators in each file.
// The app.ts import above registers <wisper-app>, which is the bootstrap
// element declared in index.html.
