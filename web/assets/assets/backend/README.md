# This directory holds the Go backend binary.
#
# Run `make flutter-linux` (or flutter-darwin, flutter-windows) from the
# project root to compile and copy the Go backend here.
#
# The binary is also copied into the Flutter build bundle by build.sh.
